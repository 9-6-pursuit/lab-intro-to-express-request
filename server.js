// I’m not sure what the difference is between app.js and server.js. 
// In the test, app.js is used, so I assume the code would be the same in both files. 
// However, according to package.json, it should be server.js. 
// For now, I will use require in server.js.

require('./app');
